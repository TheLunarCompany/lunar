// Reserved server name for mcpx's built-in capabilities. Lives here (not in
// `internal-tools/`) so `services/` can import without a services →
// internal-tools cycle.
export const INTERNAL_SERVICE_NAME = "mcpx";
