# Copy this to .env.local and modify as needed
VITE_WS_URL=ws://127.0.0.1:9001
VITE_MCPX_SERVER_PORT=9523  # needs to be the same as the MCPX_PORT in mcpx-server/.env
VITE_MCP_JSON_FILE_PATH=file:///mcp.json
VITE_MSW_ENABLED=false